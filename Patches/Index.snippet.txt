<!-- Index.razor: clases visuales -->
<h1 class="h1"><strong>Printing Jobs</strong></h1>

<div class="toolbar">
  <label>Status:</label>
  <select class="select" @bind="SelectedStatus">
    <option value="">(All)</option>
    @foreach (var s in Statuses) { <option value="@s">@s</option> }
  </select>
</div>

<table class="table">
  <!-- columnas/filas -->
</table>
